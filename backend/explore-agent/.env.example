# Copy this to .env and paste the SAME Groq key the Next.js app already uses
# (frontend/.env.local -> GROQ_API_KEY). Do not create a new key.
GROQ_API_KEY=

# Optional: only set if the Next.js app runs somewhere other than the dev default.
# NEXTJS_BASE_URL=http://localhost:3000
